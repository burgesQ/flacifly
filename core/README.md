# core

Shared base package for flacifly: coloured logging, the SQLite persistence layer
(dedup + review queue), the base `Config` dataclass, shared types, exit codes, and
filesystem locking. Imported by `fetcher` and `tagger`.
