"""Exit codes for the fetcher CLI (shared codes re-exported from core)."""

from core.exit_codes import (  # noqa: F401
    CLI_ERROR,
    INVALID_PATH,
    LOCKED,
    PROCESSING_ERROR,
    SUCCESS,
)

DOWNLOAD_ERROR = 7
