# fetcher

Task 1. Downloads best-quality audio from YouTube / YouTube Music / SoundCloud via
`yt-dlp`, keeps the original lossy file, and transcodes a FLAC copy for CDJ use.
Records every track in the shared SQLite DB for dedup. CLI: `flacifly-fetch`.
