"""Exit codes for the tagger CLI (shared codes re-exported from core)."""

from core.exit_codes import (  # noqa: F401
    CLI_ERROR,
    INVALID_PATH,
    LOCKED,
    PROCESSING_ERROR,
    SUCCESS,
)

TAG_ERROR = 8
